import { RulesTransformPipe } from './rules-transform.pipe';

describe('RulesTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new RulesTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
