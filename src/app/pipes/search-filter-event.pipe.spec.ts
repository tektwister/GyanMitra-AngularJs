import { SearchFilterEventPipe } from './search-filter-event.pipe';

describe('SearchFilterEventPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterEventPipe();
    expect(pipe).toBeTruthy();
  });
});
