import { ResolvecollegePipe } from './resolvecollege.pipe';

describe('ResolvecollegePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvecollegePipe();
    expect(pipe).toBeTruthy();
  });
});
